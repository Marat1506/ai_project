## Установка и запуск

### Требования

- **Node.js** 18+ или **Bun** 1.0+
- **Git**
- **OpenAI API ключ**

### Шаги установки

1. **Клонируйте репозиторий:**

   ```bash
   git clone https://github.com/Marat1506/ai_project
   cd ai_project
   ```

2. **Установите зависимости:**

   **Вариант A: С Bun (рекомендуется):**

   ```bash
   bun install
   ```

   **Вариант B: С npm (если проблемы с Bun):**

   ```bash
   npm install
   ```

   **Вариант C: С yarn:**

   ```bash
   yarn install
   ```

3. **Создайте файл `.env.local`** в корне проекта:

   ```env
   OPENAI_API_KEY=your_openai_api_key_here
   ```

4. **Запустите dev сервер:**

   **С Bun:**

   ```bash
   bun run dev
   ```

   **С npm:**

   ```bash
   npm run dev
   ```

   **С yarn:**

   ```bash
   yarn dev
   ```



## Функциональность

### 1. Чат с тредами ✅

- ✅ Список тредов слева (как в ChatGPT)
- ✅ Создание нового треда
- ✅ Переключение между тредами без потери данных
- ✅ Загрузка истории сообщений из базы
- ✅ Сохранение всех сообщений (user/assistant) в SQLite
- ✅ Сохранение tool invocations в базе данных

### 2. Client-side Tools (Generative UI) ✅

- ✅ `readExcelRange` - чтение диапазона ячеек из Excel
- ✅ `updateExcelCell` - обновление ячейки (с подтверждением)
- ✅ `getCellFormula` - получение и объяснение формулы
- ✅ `confirmAction` - запрос подтверждения опасных действий

### 3. Подтверждение опасных действий ✅

- ✅ UI-диалоги для подтверждения update/delete операций
- ✅ Кнопки "Да" / "Нет" в интерактивных компонентах
- ✅ Действие выполняется только после подтверждения пользователем
- ✅ Отмена действий через кнопку "Нет"

### 4. Интерактивная работа с XLSX таблицей ✅

- ✅ Чтение диапазонов ячеек (`Sheet1!A1:B3`)
- ✅ Запись в ячейки и диапазоны (с подтверждением)
- ✅ Объяснение формул из ячеек
- ✅ **Модальное окно для просмотра таблицы**
- ✅ **Выделение ячеек мышкой в модальном окне**
- ✅ **Создание меншонов диапазонов (`@Sheet1!A1:B3`)**
- ✅ **Сохранение диапазонов в localStorage**
- ✅ **Кликабельные ссылки "📊 Показать таблицу"**
- ✅ **Интеграция меншонов с агентом для работы с таблицей**

## Использование

### Работа с тредами

1. Нажмите "+ Новый тред" для создания нового треда
2. Кликните на тред в списке для переключения
3. Все сообщения автоматически сохраняются в базу
4. История сообщений загружается при переключении тредов

### Интерактивная работа с Excel таблицей

1. **Чтение таблицы:**

   - Спросите: "Покажи мне данные из диапазона Sheet1!A1:E7"
   - Агент вызовет tool и покажет кликабельную ссылку "📊 Показать таблицу"
   - Кликните на ссылку для открытия интерактивного модального окна

2. **Выделение ячеек и создание меншонов:**

   - В модальном окне выделите нужные ячейки мышкой (drag & drop)
   - Нажмите "Добавить диапазон в чат"
   - Диапазон будет вставлен в поле ввода как меншон `@Sheet1!A1:B3`

3. **Использование меншонов:**

   - Нажмите на иконку таблицы рядом с полем ввода для просмотра сохраненных диапазонов
   - Или используйте меншон напрямую: "Объясни формулу в @Sheet1!D4"
   - Агент автоматически распознает меншоны и использует соответствующие tools

4. **Обновление и удаление данных:**
   - Попросите агента обновить: "Обнови Sheet1!A2 значением 'test'"
   - Или удалить: "@Sheet1!A1:C1 удали эти ячейки"
   - Появится диалог подтверждения с кнопками "Да" / "Нет"
   - Нажмите "Да" для выполнения или "Нет" для отмены

### Примеры команд

```
Покажи мне данные из диапазона Sheet1!A1:E7
@Sheet1!A1:C1 удали эти ячейки
Обнови ячейку Sheet1!B2 значением "Новое значение"

```

## База данных

База данных SQLite создается автоматически при первом запуске в `data/chat.db`.

**Таблицы:**

- `threads` - треды (id, title, created_at, updated_at)
- `messages` - сообщения (id, thread_id, role, content, tool_invocations, created_at)

**Инициализация БД:**
База данных инициализируется автоматически при первом запуске приложения. Миграции выполняются автоматически.

## Что реализовано

### ✅ Полностью реализовано:

- **Чат с тредами и персистентностью** - полная функциональность переключения тредов, сохранения истории
- **Client-side tools с Generative UI** - все инструменты работают корректно
- **Подтверждение опасных действий через UI** - интерактивные диалоги с кнопками "Да"/"Нет"
- **Работа с XLSX (чтение, запись, формулы)** - полная интеграция с Excel файлами
- **Интерактивное модальное окно для таблицы** - просмотр таблицы с выделением ячеек
- **Меншоны диапазонов и интеграция с агентом** - создание и использование `@Sheet1!A1:B3`
- **Кликабельные ссылки "📊 Показать таблицу"** - появляются после использования readExcelRange
- **Сохранение tool invocations в БД** - корректное восстановление интерактивных элементов
